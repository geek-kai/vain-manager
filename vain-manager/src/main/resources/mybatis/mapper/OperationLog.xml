<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.vain.manager.log.entity.OperationLog">

    <insert id="insert" parameterType="com.vain.manager.log.entity.OperationLog" useGeneratedKeys="true"
            keyProperty="id">
    INSERT INTO t_operation_log
    (operationType,operationData,operationIP,className,methodName,operationTime,info,exceptionMessage)
    VALUES
    (#{operationType},#{operationData},#{operationIP},#{className},#{methodName},now(),#{info},#{exceptionMessage})
    </insert>

</mapper>